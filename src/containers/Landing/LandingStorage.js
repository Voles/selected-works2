import React, { Component } from 'react';
import './Landing.scss';

class LandingStorage extends Component {
  render() {
    return (
      <div className="App-Landing LandingStorage">
        <h1 className="display-1">Designing storage solutions for real homes</h1>
        <p className="lead">A recurring challenge in interior architecture is how to design against clutter, thinking of better ways to make sure everything is tidy and accessible.</p>
      </div>
    );
  }
}

export default LandingStorage;
